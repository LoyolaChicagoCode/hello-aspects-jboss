Use Run > Run... to create the following configuration:

Main | Main class: helloworld.Main

Arguments | VM arguments

-javaagent:lib/jboss-aop-jdk50.jar
-Djboss.aop.class.path=bin

Then press Apply and Run.